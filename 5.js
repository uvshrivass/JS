// Remainder of a number without using modulus operator.

var dividend = 46;
var divisor = 7;

var quotient = parseInt(dividend/divisor);

var product = quotient * divisor

// Finding the modulus

var modulus = dividend - product

console.log( "The modulus is: ", modulus)